version: '2'
services:
  mariadb:
    container_name: mariadb
    image: linuxserver/mariadb
    networks:
      - link
    volumes:
      - ${DOCKER_HOME}/mariadb:/config
    environment:
      - PGID=${PGID}
      - PUID=${PUID}
      - TZ=${TIMEZONE}
networks:
  link:
